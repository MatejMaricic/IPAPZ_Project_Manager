{% extends 'base.html.twig' %}
{% block title %}
Main page
{% endblock %}
{% block body %}
    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('app_logout') }}">Logout</a>


    {% else %}
    <a href="{{ path('app_register') }}">Register</a>
    <a href="{{ path('app_login') }}">Login</a>
    {% endif %}
    <a href="{{ path('project_index') }}">New Project</a>
    Test
{% endblock  %}